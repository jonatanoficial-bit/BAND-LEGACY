# Hotfix v1.3.4 — Conexão dos assets organizados

Objetivo: não mudar a jogabilidade; apenas ligar os caminhos oficiais dos assets nos pontos que ainda estavam com placeholders.

Correções feitas:

- A tela `New Game` agora usa os 12 avatares reais e as 12 faces reais.
- A prévia grande da tela `New Game` agora troca conforme o avatar escolhido.
- A seleção de avatar da tela `New Game` e da tela `Perfil e Carreira` usa o mesmo save.
- Os cards do lobby recebem backgrounds por caminho oficial.
- `feature-banner` e `show-stage` recebem backgrounds oficiais por JavaScript.
- Os extras de baterista gospel foram apontados para os arquivos existentes de drummer para evitar caminhos quebrados.
- A build continua sem assets pesados; basta colocar a pasta `assets/` organizada na raiz.

Caminho esperado:

```text
BAND-LEGACY-main/assets/...
```
